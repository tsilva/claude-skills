[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "claude-skills"
version = "0.1.0"
description = "**🧩 A curated collection of Claude Code skills that supercharge your AI-assisted development workflow**"
requires-python = ">=3.10"
dependencies = []
